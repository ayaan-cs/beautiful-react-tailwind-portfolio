# Content and agent notes

Read these before changing copy or adding a metric:

1. `content-rules.md` — honesty contract. Fail closed.
2. `src/content/` — the only factual source (`profile.js`, `projects.js`, `experience.js`, `metrics.js`, `certificates.js`, `skills.js`, `proof.js`).
3. This site stays on Vite + React + Tailwind v4 so the existing Netlify deploy (`ayaansportfolio.netlify.app`) keeps working. Structure follows a portfolio build brief: typed content, dark-first tokens, model cards, proof strip, command menu, scorecard playground.

Do not invent benchmarks. If a real export does not exist, ship a labelled empty state.
